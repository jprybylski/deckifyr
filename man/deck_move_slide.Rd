% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slides.R
\name{deck_move_slide}
\alias{deck_move_slide}
\title{Reorder a slide in a presentation.yaml}
\usage{
deck_move_slide(presentation, id, after = NULL, before = NULL, index = NULL)
}
\arguments{
\item{presentation}{Path to \code{presentation.yaml}.}

\item{id}{The slide's id.}

\item{after, before, index}{At most one of these controls the new
position: immediately \code{after}/\code{before} another slide's id, or a
0-based \code{index}. \code{after}/\code{before} naming \code{id} itself is an error
(there's no other copy of the slide to be relative to).}
}
\value{
The CLI result (invisibly).
}
\description{
Reorder a slide in a presentation.yaml
}
\examples{
\dontrun{
# See deck_add_slide()'s example for setting up a scratch project_dir.
presentation <- file.path(project_dir, "presentation.yaml")
deck_move_slide(presentation, id = "new-slide", index = 0)
}
}
