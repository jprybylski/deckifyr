% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{deck_validate}
\alias{deck_validate}
\title{Validate a deckifyr project}
\usage{
deck_validate(presentation, strict = TRUE)
}
\arguments{
\item{presentation}{Path to \code{presentation.yaml}.}

\item{strict}{Reject unitless geometry when \code{TRUE} (default); \code{FALSE}
is the \code{--warn-only} policy from spec section 11.1.}
}
\value{
A parsed list from the CLI's JSON output, e.g. \verb{$valid},
\verb{$slide_count}, \verb{$layout_count}.
}
\description{
Loads and validates \code{presentation.yaml} plus its referenced
\code{design.yaml}/\code{layouts.yaml} (schema shape, layout references, and
element box unit strings). Does not merge layouts onto slides or
resolve content -- see \code{deckifyr.cli}'s module docstring in the
bundled Python source for what validation currently does and does not
cover.
}
\examples{
\dontrun{
# Validate the bundled minimal example project (requires a provisioned
# Python/uv/pyro toolchain -- see vignette("getting-started")).
presentation <- system.file(
  "examples", "minimal-deck", "presentation.yaml",
  package = "deckifyr"
)
deck_validate(presentation)
}
}
