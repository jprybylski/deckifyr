% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slides.R
\name{deck_update_slide}
\alias{deck_update_slide}
\title{Update an existing slide's layout, notes, or elements}
\usage{
deck_update_slide(
  presentation,
  id,
  layout = NULL,
  notes = NULL,
  elements = NULL
)
}
\arguments{
\item{presentation}{Path to \code{presentation.yaml}.}

\item{id}{The slide's id.}

\item{layout}{New layout name, \code{NA} to clear it (freeform), or \code{NULL}
(default) to leave it unchanged.}

\item{notes}{New speaker notes text, \code{NA} to remove any notes, or
\code{NULL} (default) to leave them unchanged.}

\item{elements}{A list to replace the slide's \code{elements} block with
(same JSON encoding as \code{\link[=deck_add_slide]{deck_add_slide()}}'s \code{elements}), or \code{NULL}
(default) to leave it unchanged.}
}
\value{
The CLI result (invisibly).
}
\description{
Only the arguments you pass are changed; anything else on the slide is
left as-is. \code{layout: null} (freeform) and "no notes" are both
meaningful, valid values in their own right (spec section 7.6) -- so
\code{NULL} can't double as this function's own "leave alone" default the
way it usually would in R. Pass \code{NA} instead to explicitly clear
\code{layout}/\code{notes}; \code{NULL} (the default) leaves that field untouched.
}
\examples{
\dontrun{
# See deck_add_slide()'s example for setting up a scratch project_dir.
presentation <- file.path(project_dir, "presentation.yaml")
deck_update_slide(presentation, id = "new-slide", notes = "Remember to mention Q3.")
}
}
