% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{.kill_deckifyr_server_pid}
\alias{.kill_deckifyr_server_pid}
\title{Kill a \verb{deckifyr serve} process found by PID, plus its \verb{uv run} parent}
\usage{
.kill_deckifyr_server_pid(pid)
}
\arguments{
\item{pid}{The listening process's PID.}
}
\description{
\code{pid} is the \emph{listening} process (\verb{python -m deckifyr}/uvicorn, what
\code{.pids_listening_on_port()} finds) -- its parent is ordinarily the
\verb{uv run} wrapper that spawned it (confirmed directly, see
\code{deck_stop_server()}'s own docs on why \code{kill_tree()} exists at all).
Since this PID wasn't necessarily launched by this R session, there's
no \code{processx::process} handle and thus no \code{kill_tree()} to call --
this walks up to the parent manually instead, only killing it if
\emph{its} command line also looks like a deckifyr server
(\code{.pid_looks_like_deckifyr_server()}), so a coincidental unrelated
parent process is never touched.
}
\keyword{internal}
