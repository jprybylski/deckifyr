% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-python.R
\name{.run_deckifyr_cli}
\alias{.run_deckifyr_cli}
\title{Invoke the bundled Python CLI via pyro}
\usage{
.run_deckifyr_cli(args)
}
\arguments{
\item{args}{Character vector of CLI arguments \emph{after} \code{deckifyr},
e.g. \code{c("validate", "presentation.yaml")}.}
}
\value{
A parsed list from the CLI's JSON output.
}
\description{
Internal helper every exported \verb{deck_*()}/\code{initialize_deck_project()}
function funnels through -- the single bridge point, so schema/merge/
compiler logic is never reimplemented in R (see CLAUDE.md's "one
engine, two facades" note). Always requests \code{--json} output so
results come back as a parsed R list (spec section 11.2: "Return
structured R lists parsed from CLI JSON") rather than needing prose
scraping.
}
\details{
\strong{Why this isn't a thin wrapper around \code{pyro::run_python_script()}:}
confirmed against a real pyro install, \code{run_python_script()} calls
\code{processx::run(..., error_on_status = TRUE)} and, on any non-zero
exit, discards the captured stdout/stderr entirely and raises a bare
\code{"<script_name> failed."} -- so the deckifyr CLI's own diagnostic JSON
(which spec section 11.1 requires it to emit on schema/resolution/
composition failure, exit code != 0) would otherwise be lost. The fix
is \code{stderr_callback}: processx invokes it per output chunk \emph{while the
process is still running}, before the exit-status check fires, so
output captured that way survives even when the call ultimately
errors. This only works because \code{deckifyr.cli} deliberately writes
its JSON error payload to stderr rather than stdout on the error path
-- see the matching comment in \code{inst/python/deckifyr/cli.py}'s
\code{main()}. Don't change one side of this handshake without the other.
}
\keyword{internal}
