% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run-python.R
\name{.handle_missing_dependency}
\alias{.handle_missing_dependency}
\title{Print install guidance for a missing external binary dependency}
\usage{
.handle_missing_dependency(dependency)
}
\arguments{
\item{dependency}{A list with \code{name}/\code{display_name}/\code{install_url},
parsed straight from the CLI's JSON \code{dependency} object.}
}
\description{
Called from \code{.run_deckifyr_cli()} when the Python CLI's JSON error
payload carries a \code{dependency} object (\code{code == "E_MISSING_DEPENDENCY"},
\code{deckifyr.schema.errors.MissingDependencyError} -- see its own
docstring) -- today, a missing \code{soffice} (LibreOffice, \code{deck_preview()})
or \code{quarto} (Quarto, any build touching a \code{type: quarto} element)
binary. Always prints a \code{cli}-formatted panel naming the dependency
and its official download page; on macOS, with Homebrew already on
\code{PATH}, in an interactive session, additionally offers to run the
known-correct \verb{brew install --cask <name>} command itself (no \code{sudo}
needed, so safe to run without extra privilege escalation) -- every
other platform, or a non-interactive session (this repo's own test
suite included, so tests never actually shell out to Homebrew), or a
machine without Homebrew, just gets the printed URL: there's no
single install command deckifyr can verify in advance for apt/winget/
etc, and this deliberately never guesses one or runs anything that
needs \code{sudo}. A failed/declined/skipped install attempt does not
retry the original \code{deckifyr} command -- the caller's own \code{stop()}
(right after this runs) still fires either way, so the user re-runs
their command themselves once the dependency is actually installed;
this machine may also have no network access to Homebrew's own
servers at all (firewalled/offline), so the install attempt is
wrapped in \code{tryCatch()} and reported as a failure rather than
crashing this function.
}
\keyword{internal}
