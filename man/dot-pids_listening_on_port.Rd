% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{.pids_listening_on_port}
\alias{.pids_listening_on_port}
\title{Find the PID(s) of whatever process is listening on \code{port}}
\usage{
.pids_listening_on_port(port)
}
\arguments{
\item{port}{The port to look up.}
}
\value{
An integer vector of PIDs (usually length 0 or 1).
}
\description{
Shells out to \code{lsof} (macOS/Linux) or PowerShell's
\code{Get-NetTCPConnection} (Windows, unverified on a real Windows machine
-- everything else in this file's port-by-PID helpers was confirmed
against a real spawned process on macOS, see \code{deck_stop_server()}'s
own docs) since neither base R nor \code{processx} expose a "who's
listening on this port" lookup -- there's no \code{processx::process}
handle to ask when the server wasn't launched by \emph{this} R session
(a lost/overwritten \code{deckifyr_server} object, or a fresh session
entirely).
}
\keyword{internal}
