% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/slides.R
\name{deck_add_slide}
\alias{deck_add_slide}
\title{Add a new slide to a presentation.yaml}
\usage{
deck_add_slide(
  presentation,
  id,
  layout = NULL,
  notes = NULL,
  elements = NULL,
  after = NULL,
  before = NULL,
  index = NULL
)
}
\arguments{
\item{presentation}{Path to \code{presentation.yaml}.}

\item{id}{The new slide's unique id.}

\item{layout}{The layout name to use, or \code{NULL} (default) for a
freeform slide (\code{layout: null}).}

\item{notes}{Speaker notes text, or \code{NULL} (default) for none.}

\item{elements}{A (typically named, keyed by element id) list to use
as the slide's \code{elements} block, or \code{NULL} (default) to add none
yet. Encoded as JSON en route to the CLI, so nested boxes/styles are
written exactly as given -- a named list becomes a JSON object
(matching a layout-driven slide's \verb{elements: \{title: ..., ...\}}
form), an unnamed list becomes a JSON array (matching a freeform
slide's \verb{elements: [\{id: ..., ...\}, ...]} form).}

\item{after, before, index}{At most one of these controls placement:
immediately \code{after}/\code{before} an existing slide id, or at a 0-based
\code{index}. Default (all \code{NULL}) appends the slide at the end.}
}
\value{
The CLI result (invisibly).
}
\description{
Validates the edited \code{presentation.yaml} (including, when \code{layout} is
set, that it names a real entry in the project's \code{layouts.yaml})
before writing -- see \code{\link[=deck_set_config]{deck_set_config()}}'s own docstring for the same
validate-before-write guarantee.
}
\examples{
\dontrun{
# Copy the bundled example into a scratch directory first -- slide
# commands write to `presentation` in place.
project_dir <- file.path(tempdir(), "my-deck")
dir.create(project_dir)
file.copy(
  list.files(
    system.file("examples", "minimal-deck", package = "deckifyr"),
    full.names = TRUE
  ),
  project_dir
)
presentation <- file.path(project_dir, "presentation.yaml")
deck_add_slide(presentation, id = "new-slide", layout = "blank")
}
}
