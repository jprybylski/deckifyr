% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{deck_build}
\alias{deck_build}
\title{Build a presentation}
\usage{
deck_build(presentation, strict = TRUE)
}
\arguments{
\item{presentation}{Path to \code{presentation.yaml}.}

\item{strict}{Reject unitless geometry when \code{TRUE} (default).}
}
\value{
A parsed list from the CLI's JSON output, including \code{output}
(the written \code{.pptx} path), \code{manifest} (the manifest path, or \code{NULL}
if \code{presentation.yaml}'s \code{build.manifest} is unset), and
\code{slide_count}.
}
\description{
Delegates to \verb{deckifyr build} in the bundled Python engine: plans
(\code{deckifyr.plan}) and composes (\code{deckifyr.pptx}, spec section 10) a
\code{.pptx} and manifest for \code{text}/\code{markdown}/\code{image}/\code{shape}/\code{group}/
\code{table} elements. Still errors for element types the compositor
doesn't support yet (quarto/reportifyr -- see spec section 18).
}
