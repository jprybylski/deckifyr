% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{.launch_server_process}
\alias{.launch_server_process}
\title{Launch the \verb{deckifyr serve} background process}
\usage{
.launch_server_process(uv_path, args, env_vars)
}
\arguments{
\item{uv_path}{Path to the \code{uv} binary, from \code{pyro::get_venv_uv_paths()}.}

\item{args}{CLI arguments to pass to \code{uv} (\code{c("run", "-m", "deckifyr", "serve", ...)}).}

\item{env_vars}{Environment for the subprocess, in \code{processx}'s own
\code{c("current", NAME = value, ...)} form.}
}
\value{
A \code{processx::process} object, not yet awaited.
}
\description{
Isolated behind its own function -- rather than calling
\code{processx::process$new()} directly inside \code{\link[=deck_serve]{deck_serve()}} -- specifically
so it's mockable: \code{testthat::local_mocked_bindings()} can only replace a
binding that already exists as a named object in a namespace/frame, and
\code{process$new} is an R6 generator method, not a plain function binding, so
it can't be intercepted that way (confirmed directly: mocking \code{new} via
\code{.package = "processx"} errors with "Can't find binding for \code{new}",
since \code{processx}'s own namespace never binds a bare \code{new} symbol). This
function is the one line that actually calls it, so tests fake this
function instead via \code{local_mocked_bindings(.launch_server_process = ..., .env = environment())} -- the same "wrap the unmockable call in a small
internal function" seam this file's own \code{.wait_for_server()} uses for
\code{socketConnection()}, just applied to an R6 method instead of a base
function.
}
\details{
Its own body is marked \verb{# nocov} for the same reason it exists at all:
every unit test replaces this whole function (there's nothing finer to
mock, unlike e.g. \code{.run_deckifyr_cli()}'s own call to
\code{pyro::run_python_script()} -- an ordinary function, directly mockable
at the call site, so \code{.run_deckifyr_cli()}'s own body stays covered by
its mocked-pyro unit tests). The real \code{processx::process$new()} call
here only executes for real inside \code{tests/testthat/test-wiring.R}'s
gated end-to-end block, which needs a real \code{.venv}/\code{uv}/\code{pyro} install
and correctly skips without one -- including in \code{test-coverage.yaml}'s
own CI job, per this repo's already-established "an honest, accepted
coverage gap, not a problem to hide" precedent (CLAUDE.md).
}
\keyword{internal}
