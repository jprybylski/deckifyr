% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/config.R
\name{deck_set_config}
\alias{deck_set_config}
\title{Write a config value into a design/layouts/presentation YAML file}
\usage{
deck_set_config(
  file,
  path,
  value,
  as_string = FALSE,
  type = c("auto", "design", "layouts", "presentation")
)
}
\arguments{
\item{file}{Path to a \code{design.yaml}, \code{layouts.yaml}, or
\code{presentation.yaml}.}

\item{path}{Dotted path to write, same syntax as \code{\link[=deck_get_config]{deck_get_config()}}.}

\item{value}{The value to write.}

\item{as_string}{Treat \code{value} as a literal string rather than parsing
it as JSON. Default \code{FALSE}.}

\item{type}{Which schema to validate the edited document against.
\code{"auto"} (the default) detects it from the document's own top-level
keys.}
}
\value{
The parsed CLI result (invisibly).
}
\description{
\code{value} is parsed as JSON by default -- \code{"true"}/\code{"12"}/\code{"[1, 2]"}
become their native typed values -- so a plain word, hex color, or
font name (\code{"Arial"}, \code{"#2457A6"}) needs no quoting and is written as
a literal string. Use \code{as_string = TRUE} for the rare case a value
would otherwise parse as something other than a string (writing the
literal text \code{"true"} or \code{"null"}, for instance). The edited document
is validated against its schema -- and, for \code{presentation.yaml}, any
changed \code{slide.layout} is cross-checked against the project's
\code{layouts.yaml} -- \emph{before} anything is written to \code{file}, so a bad
edit never corrupts it.
}
\examples{
\dontrun{
# Copy the bundled example into a scratch directory first -- deck_set_config()
# writes to `file` in place.
project_dir <- file.path(tempdir(), "my-deck")
dir.create(project_dir)
file.copy(
  system.file("examples", "minimal-deck", "design.yaml", package = "deckifyr"),
  file.path(project_dir, "design.yaml")
)
deck_set_config(file.path(project_dir, "design.yaml"), "colors.primary", "#2457A6")
}
}
