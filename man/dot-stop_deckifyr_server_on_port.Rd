% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/serve.R
\name{.stop_deckifyr_server_on_port}
\alias{.stop_deckifyr_server_on_port}
\title{Stop whatever deckifyr server is listening on \code{host}:\code{port}, if any}
\usage{
.stop_deckifyr_server_on_port(host, port)
}
\arguments{
\item{host, port}{The address to check.}
}
\value{
\code{TRUE} if something was found and killed, \code{FALSE} if nothing
was listening there at all.
}
\description{
Shared by \code{\link[=deck_stop_server]{deck_stop_server()}}'s \verb{port =} form and \code{\link[=deck_serve]{deck_serve()}}'s
\code{force = TRUE} -- both need the same "find it, verify it's ours,
kill it" sequence. Refuses (via \code{stop()}) to kill anything whose
command line doesn't look like a deckifyr server, per-PID; never
silently skips a non-deckifyr occupant, since that would leave the
caller thinking the port is clear when it isn't.
}
\keyword{internal}
